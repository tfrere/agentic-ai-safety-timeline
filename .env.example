# Copy to .env for local runs. On GitHub Actions this is a repository secret.
OPENROUTER_API_KEY=sk-or-v1-...

# Carrier for OpenRouter's web plugin (same default as marseille-agenda).
WATCH_MODEL=openai/gpt-5.4-mini
